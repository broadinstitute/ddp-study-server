{
  author = "bskinner"
  description = "DDP-8530: Update PEX expressions to resolve correctly when in the DEPENDENT workflow"
  changes = [
    {
      blockType = "QUESTION"
      stableId = "WHAT_IS_RELATIONSHIP"

      expected {
        shownExpr = """user.studies["singular"].forms["ABOUT_HEALTHY"].questions["ABOUT_HEALTHY_WHO_ENROLLING_HIDDEN"].answers.hasOption("MYSELF")"""
      }

      new {
          "shownExpr": """!(user.studies["singular"].forms["ABOUT_HEALTHY"].questions["ABOUT_HEALTHY_WHO_ENROLLING_HIDDEN"].answers.hasOption("CHILD"))"""
      }
    },
    {
      blockType = "QUESTION"
      stableId = "CHD_OTHER_MYSELF"

      expected {
        shownExpr = """
                user.studies["singular"].forms["ABOUT_HEALTHY"].questions["HAVE_CHD"].answers.hasOption("CHD_OTHER")
                && user.studies["singular"].forms["ABOUT_HEALTHY"].questions["ABOUT_HEALTHY_WHO_ENROLLING_HIDDEN"].answers.hasOption("MYSELF")
              """
      }

      new {
          "shownExpr": """!(user.studies["singular"].forms["ABOUT_HEALTHY"].questions["ABOUT_HEALTHY_WHO_ENROLLING_HIDDEN"].answers.hasOption("CHILD"))"""
      }
    }
  ]
}