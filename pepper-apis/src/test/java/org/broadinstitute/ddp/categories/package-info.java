/**
 * Put junit test categorizers (see https://github.com/junit-team/junit/wiki/Categories)
 * here.  We'll use test categorizers to help create intellij and CI test runner
 * configurations that let us do things like "run the fast test", "run only the slow tests",
 * "run just the ui tests", etc.
 */
package org.broadinstitute.ddp.categories;