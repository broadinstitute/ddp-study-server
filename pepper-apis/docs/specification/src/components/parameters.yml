studyGuid:
  in: path
  name: study
  required: true
  description: the study guid
  schema:
    type: string
userGuid:
  in: path
  name: user
  required: true
  description: the user's guid
  schema:
    type: string
instanceGuid:
  in: path
  name: instance
  required: true
  description: the activity instance guid
  schema:
    type: string
questionStableId:
  in: path
  name: question
  required: true
  description: the question stableId
  schema:
    type: string
